# GitHub Personal Access Token (optional)
# To fetch pinned repositories, create a token at:
# https://github.com/settings/tokens
# Required scope: read:user, public_repo
#
# Without this token, the app will fetch your most recent repositories
VITE_GITHUB_TOKEN=your_github_token_here
